name: crystal_grimoire
description: "🔮 Your mystical crystal identification and spiritual guidance companion"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Core dependencies
  cupertino_icons: ^1.0.6
  http: ^1.1.0
  
  # Image handling
  image_picker: ^1.0.4
  image: ^4.1.3
  
  # Storage & persistence
  shared_preferences: ^2.2.2
  path_provider: ^2.1.1
  sqflite: ^2.3.0
  
  # State management
  provider: ^6.1.1
  
  # UI enhancements
  flutter_staggered_animations: ^1.1.1
  shimmer: ^3.0.0
  lottie: ^2.7.0
  
  # Utilities
  uuid: ^4.1.0
  crypto: ^3.0.3
  intl: ^0.18.1
  
  # Camera
  camera: ^0.10.5+5
  
  # Subscriptions (for future implementation)
  # purchases_flutter: ^6.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/animations/
    - assets/icons/
  
  # fonts:
  #   - family: Mystical
  #     fonts:
  #       - asset: assets/fonts/Cinzel-Regular.ttf
  #       - asset: assets/fonts/Cinzel-Bold.ttf
  #         weight: 700